Drop your personal photos here (e.g. memory-1.jpg) and reference them
from `src/data/birthdayData.ts` by importing them at the top of the file:

  import memory1 from "../assets/photos/memory-1.jpg";

then use `memory1` as the `imagePlaceholder` value instead of the Unsplash URL.
